# Frontend environment variables
# Set this to the base URL of your backend API in production
VITE_API_URL=https://your-backend-service.up.railway.app

# When deploying the frontend separately, use VITE_API_URL to point to your backend.
